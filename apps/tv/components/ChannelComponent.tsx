'use client'

import { Suspense } from 'react'

export function ChannelComponent({ curatedMedia }: { curatedMedia: string[] }) {
  // threeJS

  return (
    <div className=''>
      <Suspense>{/* threeJS */}</Suspense>
    </div>
  )
}
